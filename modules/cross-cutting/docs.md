# Documentation updates

- Update README or module docs when user-facing behavior, setup steps, or CLI flags change.
- Prefer short examples that compile or run over abstract prose when onboarding is the goal.
- Link to upstream docs for framework basics instead of copying them wholesale.
